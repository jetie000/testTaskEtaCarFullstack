{
    "name": "packages",
    "version": "1.0.0",
    "main": "index.js",
    "license": "MIT",
    "private": true,
    "scripts": {
        "start": "concurrently \"wsrun --parallel start\"",
        "build": "concurrently \"wsrun --parallel build\""
    },
    "workspaces": [
        "packages/*"
    ],
    "devDependencies": {
        "concurrently": "^5.3.0",
        "wsrun": "^5.2.0"
    }
}
